import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const calendar = new Vuex.Store({
  state: {
    events: {},
  },

  getters: {
  },

  mutations: {
  },

  actions: {
  },
});

export default calendar;
